(function(window) {

    var baseUrl = 'http://';

    window.config = {
        auth: {
            baseUrl: baseUrl + '/auth'
        },
        api: {
            baseUrl: baseUrl + '/api'
        }
    };
})(window);
